# ParticlesJS
Lightweight, documented particle engine for the browser: renders animated shapes/images on a canvas with density clamping, spatial grid line‑linking, and interactive modes (grab, bubble, repulse, push, remove). Includes presets (rain, snow, confetti) and ES6/global builds.
